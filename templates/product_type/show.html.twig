{% extends 'base.html.twig' %}

{% block title %}ProductType{% endblock %}

{% block body %}
    <h1>ProductType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_type.id }}</td>
            </tr>
            <tr>
                <th>Type_name</th>
                <td>{{ product_type.typeName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_type_index') }}">back to list</a>

    <a href="{{ path('product_type_edit', {'id': product_type.id}) }}">edit</a>

    {{ include('product_type/_delete_form.html.twig') }}
{% endblock %}
