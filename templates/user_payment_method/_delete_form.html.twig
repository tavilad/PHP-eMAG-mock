<form method="post" action="{{ path('user_payment_method_delete', {'id': user_payment_method.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_payment_method.id) }}">
    <button class="btn">Delete</button>
</form>
